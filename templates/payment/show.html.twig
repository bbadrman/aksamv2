{% extends 'base.html.twig' %}

{% block title %}Payment{% endblock %}

{% block body %}
    <h1>Payment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>Frais</th>
                <td>{{ payment.frais }}</td>
            </tr>
            <tr>
                <th>Cotisation</th>
                <td>{{ payment.cotisation }}</td>
            </tr>
            <tr>
                <th>CreatAt</th>
                <td>{{ payment.creatAt ? payment.creatAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_index') }}">back to list</a>

    <a href="{{ path('app_payment_edit', {'id': payment.id}) }}">edit</a>

    {{ include('payment/_delete_form.html.twig') }}
{% endblock %}
