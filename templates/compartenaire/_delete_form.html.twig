<form method="post" action="{{ path('app_compartenaire_delete', {'id': compartenaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compartenaire.id) }}">
    <button class="btn">Delete</button>
</form>
